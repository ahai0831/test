bool save2local(const char* sName, const char* sLocalPath);
函数返回值为布尔值1为创建成功，0为创建失败，参数为（char* :文件名，char* :本地路径）；
其中：
	文件名没有后缀名时会自动添加txt后缀，即文件名为*，则添加后为*.txt，
	本地路径需要使用双转义字符，即D:\\a\\b，或者单个斜杠字符，即D:/a/b，之后会在本地路径下创建该文件，并返回是否成功；